import OpenGraphImage, { size, contentType } from "../../opengraph-image";

export { size, contentType };

/** Screen-reader / social alt. Pixel art stays Latin — Satori cannot shape Arabic fonts. */
export const alt =
  "ثرفت إت — أزياء فاخرة مستعملة في دبي. انضمّي لقائمة الانتظار.";

export default OpenGraphImage;
